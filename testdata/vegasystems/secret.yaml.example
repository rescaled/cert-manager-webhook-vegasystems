# Copy to secret.yaml and fill in real credentials before running the
# conformance suite. The secret.yaml file itself is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: vegasystems-credentials
type: Opaque
stringData:
  apiUser: "REPLACE_ME"
  apiKey:  "REPLACE_ME"
